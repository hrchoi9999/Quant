# trading_sign

This workspace is dedicated to the trading timing model work for this thread.

## Scope

- All thread-specific design, code, experiments, and artifacts must stay under `D:\Quant\trading_sign`.
- Thread-specific documents must stay under `D:\Quant\trading_sign\docs`.
- Other folders such as `D:\Quant`, `D:\QuantService`, and work created by other threads are read-only reference sources for this thread.
- If this work requires changes outside `D:\Quant\trading_sign`, create a work request document instead of editing those targets directly.

## Initial structure

- `docs/`
  - working rules and boundaries
  - V1 timing model design
  - cross-thread work request templates
